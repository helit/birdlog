# /build-glossary — Stage 1: Glossary discovery

```
Skill({skill: "build-glossary"})
```

Scans code + `docs/PRD.md` + `docs/ARCHITECTURE.md` for domain terms and produces or refreshes `docs/GLOSSARY.md`. Batched interview for ambiguous candidates.

**Run when:** starting a new engagement, when new domain terms land in the PRD, or when the glossary drifts.

**Next:** `/plan-epics` or continue in-flight work.
