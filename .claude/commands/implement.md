# /implement — Phase 3: Implement

**Usage:** `/implement <N>`

```
Skill({skill: "implement"})
```

Delegates tasks to Codex; Claude supervises. TDD enforced. One task at a time.

**Prerequisite:** Phase 2 complete, spec explicitly approved.
**Next:** `/clear`, then `/test <N>`
