# /initiate-project

```
Skill({skill: "initiate-project"})
```

**Run once per engagement, before any `/define` calls.**

Per-category approval: Language → Frontend → Backend → Database → Tests → Tooling.
Scaffolds the project, updates `CLAUDE.md` and `docs/architecture/system-overview.md`.
