# /define-prd — Stage 1: Product requirements

```
Skill({skill: "define-prd"})
```

Produces or refreshes `docs/PRD.md`. Takes a source document (file path / URL / freeform description) or runs a Socratic greenfield interview.

**Run when:** starting a new engagement, or when product scope shifts significantly.

**Next:** `/build-glossary` (if first-time setup), then `/plan-epics`.
