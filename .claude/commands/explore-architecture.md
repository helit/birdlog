# /explore-architecture — Stage 1: Architecture discovery

```
Skill({skill: "explore-architecture"})
```

Scans the repo and produces or refreshes `docs/ARCHITECTURE.md`. Diff-and-patch if the file already exists; otherwise creates it from scratch.

**Run when:** starting a new engagement, after significant structural changes, or when the doc has drifted from reality.

**Next (first-time setup):** `/define-prd` → `/build-glossary` → `/plan-epics`
