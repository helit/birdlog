# /review — Phase 5: Review

**Usage:** `/review <N>`

```
Skill({skill: "review"})
```

Complexity gate → 1 agent (SIMPLE) or 3 agents (COMPLEX). All Critical findings must be resolved.

**Prerequisite:** Phase 4 complete (clean test run).
**Next:** `/clear`, then `/commit <N>`
