# /spec — Phase 2: Spec

**Usage:** `/spec <N>`

```
Skill({skill: "spec"})
```

Runs 3 specialist agents → per-category approval → writes spec to `docs/specs/`.

**Prerequisite:** Phase 1 complete (`[x] Phase 1: Define` on the issue).
**Next:** `/clear`, then `/implement <N>`
