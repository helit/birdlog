# /commit — Phase 6: Commit & PR

**Usage:** `/commit <N>`

```
Skill({skill: "commit"})
```

Final verification (test + lint) → commits → creates PR with spec link and `Closes #N`. Human merges.

**Prerequisite:** Phase 5 complete, all Critical findings resolved.
